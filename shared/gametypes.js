const TYPES = {
  EVENTS: {
    CONNECTED: 'connect',
    INITIAL: 'INITIAL',
    INITIALED: 'INITIALED',
    MOVE: 'move',
    MOVED: 'onMove'
  }
}

if (!(typeof exports === 'undefined')) {
  module.exports = TYPES
}
